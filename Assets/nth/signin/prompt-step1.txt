ok giờ hãy code tiếp cho tôi 1 template mới để cày game với flow như sau (mặc định là việc di chuyển chuột, nhập text là giống như người thật và mỗi lần nhập text xong thì đều nên chờ khoảng 2 giây mới làm hành động tiếp, việc ấn ảnh thì mặc định là click chính giữa ảnh, và sử dụng chiến lược find multi-scale cho mọi ảnh, chọn ảnh giống nhất và sát kích thước nhất):


Khi ấn play flow thì hiển thị 1 popup cho phép người dùng nhập vào sheet, dòng bắt đầu đầu tiên để bắt đầu đọc file excel

bước 1: đăng nhập (asset ở thư mục Templates/nth/signin
 + Đọc file excel có tên Nick phụ nth.xlsx trong thư mục hoặc file excel duy nhất trong thư mục với sheet và dòng được đưa vào từ người dùng (tăng dần 1 dòng qua mỗi vòng lặp) với username là cột 1, password là cột 2 của dòng
 + Tìm ảnh 01_usernamebox (chờ ảnh tối đa 5s) và di chuyển chuột tới, ấn vào ảnh
 + Nhập username, chờ 2s
 + Ấn tab, chờ 1s
 + Nhập password, chờ 2s
 + Chờ ảnh 03_loginbutton và di chuyển chuột tới, ấn vào ảnh
 + Chờ ảnh 04_checkbox tối đa 5s nếu xuất hiện thì (di chuyển chuột tới ấn vào sau đó delay 1s, chờ ảnh 05_agree xuất hiện và click vào và xuống bước tiếp) nếu 04_checkbox không xuất hiện thì xuống bước tiếp
 + Tìm ảnh 06_startbutton và di chuyển chuột tới click vào
 + Đợi ảnh 07_backbutton xuất hiện thì tìm ảnh 08_startbutton2 và di chuyển chuột tới click vào

Trước hết chỉ cần như này đã, các bước còn lại tôi sẽ nói sau

ok giờ hãy code tiếp cho tôi 1 template mới để cày game với flow như sau (mặc định là việc di chuyển chuột, nhập text là giống như người thật và mỗi lần nhập text xong thì đều nên chờ khoảng 1 giây mới làm hành động tiếp, việc ấn ảnh thì mặc định là click chính giữa ảnh và ấn ngay sau khi tìm được ảnh, và sử dụng chiến lược find multi-scale cho mọi ảnh, chọn ảnh giống nhất và sát kích thước nhất, nếu bước nào không tìm được ảnh thì bỏ qua để xuống bước tiếp theo):
Bước 4: Cài đặt tự động đánh (asset ở thư mục Templates/nth/ingame_settingcombat)
 + Ấn phím B
 + Chờ ảnh 02_settingcombat_automedicine xuất hiện và di chuyển tới click vào
 + Chờ ảnh 03_settingcombat_usemedicine xuất hiện và di chuyển tới click vào
 + ấn esc, ấn y
 + tìm ảnh 04_settingcombat_skip và di chuyển chuột tới ấn vào
 + Chờ ảnh 01_settingcombat_searchbutton xuất hiện và di chuyển tới, click vào vị trí đó 2 lần mỗi lần cách nhau 500 ms
 + lần lượt tìm ảnh 05_settingcombat_openauto và 06_settingcombat_quickswap và di chuyển chuột tới click vào mỗi ảnh
 + ấn esc
Thêm flow lên GUI của wpf

ok giờ hãy code tiếp cho tôi 1 template mới để cày game với flow như sau (mặc định là việc di chuyển chuột, nhập text là giống như người thật và mỗi lần nhập text xong thì đều nên chờ khoảng 0.5 giây mới làm hành động tiếp, việc ấn ảnh thì mặc định là click chính giữa ảnh và ấn ngay sau khi tìm được ảnh, và sử dụng chiến lược find multi-scale cho mọi ảnh, chọn ảnh giống nhất và sát kích thước nhất, nếu bước nào không tìm được ảnh thì bỏ qua để xuống bước tiếp theo):
Bước 5: Tìm đường tới rương báu (asset ở thư mục Template/nth/ingame_map)
 + ấn phím M
 + Chờ ảnh 02_map_zoomupbutton, di chuyển chuột tới và click vào vị trí đó 10 lần liên tục, cách nhau 0.2s, trong quá trình đó nếu xuất hiện ảnh 03_map_skipinstruction thì di chuột tới click vào, ấn space
 + Tìm ảnh 04_map_inputbutton di chuyển chuột tới và ấn vào
 + Có các ảnh số từ 1 tới 9 với tên map_x.png (x là số) và lần lượt tìm và click số 8 -> 5 -> 9 
 + Tìm ảnh 06_map_zindex555 và click vào chính giữa ảnh 06_map_zindex555 và ảnh 04_map_inputbutton sau đó lần lượt tìm và di chuột tới click số 9 -> 8 -> 3
 + Tìm ảnh 05_map_follow và di chuột tới click vào
 + Ấn esc
 + Delay 500ms,  ấn E 2 lần Nếu xuất hiện ảnh 07_map_ruong thì di chuyển chuột tới và ấn vào, nếu k thấy thì tìm ảnh 07_map_ruong2 nếu có thì ấn phím F. Nếu vẫn k có thì quay lại ấn E 2 lần và quay lại tìm ảnh, cứ thế nếu k thấy 2 ảnh rương thì dừng lại bước này sau 3 lần k thấy 
Thêm flow lên GUI của wpf

ok giờ hãy code tiếp cho tôi 1 template mới để cày game với flow như sau (mặc định là việc di chuyển chuột, nhập text là giống như người thật và mỗi lần nhập text xong thì đều nên chờ khoảng 0.5 giây mới làm hành động tiếp, việc ấn ảnh thì mặc định là click chính giữa ảnh và ấn ngay sau khi tìm được ảnh, và sử dụng chiến lược find multi-scale cho mọi ảnh, chọn ảnh giống nhất và sát kích thước nhất, nếu bước nào không tìm được ảnh thì bỏ qua để xuống bước tiếp theo):
Bước 6: Nhiệm vụ hàng ngày (asset ở thư mục Template/nth/ingame_daily)
 + Tìm ảnh 01_daily_openbutton trên màn hình, nếu k tìm thấy thì ấn esc và tìm lại, cứ thế 5 lần k được thì dừng
 + di chuyển chuột tới click vào
 + bắt đầu 1 vòng lặp nếu tìm thấy 1 trong các ảnh 02_daily_maintail hoặc 03_daily_adventure hoặc 04_daily_photograph hoặc 06_daily_mhl thì click vào ảnh đầu tiên tìm được, bắt đầu vòng lặp mới, nếu như không tìm thấy ảnh nào trong mấy ảnh trên thì tìm ảnh 05_daily_reset và di chuột click vào (thêm 1 điều kiện đó là hình ảnh 05_daily_reset đó không nằm trong ảnh 06_daily_mhl2 nếu nằm trong thì chọn ảnh 05_daily_reset khác), bắt đầu 1 vòng lặp mới cho tới khi click đủ 4 lần thì bắt đầu 1 vòng lặp khác: tìm ảnh 07_daily_dungoan nếu k có thì tìm ảnh 05_daily_reset và click vào cho tới khi thấy 07... thì di chuột click vào, timeout search mấy ảnh trên cứ để 0.5 giây thôi và sau mỗi lần click thì delay 0.5s
Thêm flow lên GUI của wpf


ok giờ hãy code tiếp cho tôi 1 template mới để cày game với flow như sau (mặc định là việc di chuyển chuột, nhập text là giống như người thật và mỗi lần nhập text xong thì đều nên chờ khoảng 0.3 giây mới làm hành động tiếp, việc ấn ảnh thì mặc định là click chính giữa ảnh và ấn ngay sau khi tìm được ảnh, và sử dụng chiến lược find multi-scale cho mọi ảnh, chọn ảnh giống nhất và sát kích thước nhất, nếu bước nào không tìm được ảnh thì bỏ qua để xuống bước tiếp theo):
Bước 7: Du ngoạn (asset ở thư mục Template/nth/dungoan)
+ Đợi ảnh 01_dungoan_skip với timeout 10s và di chuyển chuột tới click vào
+ Delay 0.2s và ấn space
+ Tìm ảnh 02_dungoan_maintale và di chuyển chuột tới click vào với timeout 20s
+ Chờ ảnh 03_dungoan_startevent và di chuyển ấn chuột
+ Khi ảnh 04_dungoan_nameevent còn xuất hiện và chưa xuất hiện 05_dungoan_godungeon thì mỗi nửa giây di chuột tới 04_dungoan_nameevent và click vào đó, nếu đã xuất hiện 05 thì di chuyển chuột tới 05_dungoan_godungeon
+ chờ ảnh 06_dungoan_skip xuất hiện thì di chuột tới ấn vào
+ delay 0.2s và ấn space
+ vòng lặp: ấn E liên tục mỗi 0.05s cho tới khi gặp ảnh 07_dungoan_reborn thì di chuột tới ấn vào rồi lại ấn E liên tục hoặc gặp ảnh 08_dungoan_doneevent thì di chuột tới click vào sau đó chờ ảnh dungoan_closeevent thì di chuột tới click vào
+ Ấn esc, delay 1s và click vào chính giữa màn hình 2 lần, ấn F1
+ Chờ ảnh 10_dungoan_dailydone di chuuootj tới và click vào
Thêm flow lên GUI

ok giờ hãy code tiếp cho tôi 1 template mới để cày game với flow như sau (mặc định là việc di chuyển chuột, nhập text là giống như người thật và mỗi lần nhập text xong thì đều nên chờ khoảng 0.3 giây mới làm hành động tiếp, việc ấn ảnh thì mặc định là click chính giữa ảnh và ấn ngay sau khi tìm được ảnh, và sử dụng chiến lược find multi-scale cho mọi ảnh, chọn ảnh giống nhất và sát kích thước nhất, nếu bước nào không tìm được ảnh thì bỏ qua để xuống bước tiếp theo):
Bước 8: Đăng xuất (asset ở thư mục Template/nth/signout)
 + Ấn esc
 + Tìm ảnh 01_signout_setting và di chuột tới click
 + Tìm ảnh 02_signout_signout và tìm ảnh 03_signout_signout_button nằm trong ảnh 02.. đó và di chuyển tới click vào
 + Ấn Space
 + đợi ảnh 04_signout_otheraccount xuất hiện với timeout cứ để 30s và click vào 2 lần, cách nhau 1s
 + tìm ảnh 05_signout_zingsignin và di chuột tới click vào 

ok giờ hãy tạo 1 template và thêm vào UI là tổng hợp tất cả các bước game với thứ tự:
Sign-In -> Chờ 10s -> Camera -> MHL -> Combat -> Map -> Daily -> Dungoan -> Signout