<Window x:Class="GameAutomation.UI.WPF.Dialogs.ExcelInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Excel Configuration"
        Width="420" Height="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F7FA"
        FontFamily="Segoe UI">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="NTH Login Configuration"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="#2D3436"
                   Margin="0,0,0,20"/>

        <!-- Sheet Name -->
        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="Sheet Name (leave empty for first sheet)"
                       FontSize="12"
                       Foreground="#636e72"
                       Margin="0,0,0,6"/>
            <Border Background="White" BorderBrush="#e1e8ed" BorderThickness="2" CornerRadius="8">
                <TextBox x:Name="SheetNameTextBox"
                         BorderThickness="0"
                         Padding="12,10"
                         FontSize="14"
                         Background="Transparent"/>
            </Border>
        </StackPanel>

        <!-- Start Row -->
        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <TextBlock Text="Start Row (data row number, 1 = first data row after header)"
                       FontSize="12"
                       Foreground="#636e72"
                       Margin="0,0,0,6"/>
            <Border Background="White" BorderBrush="#e1e8ed" BorderThickness="2" CornerRadius="8">
                <TextBox x:Name="StartRowTextBox"
                         Text="1"
                         BorderThickness="0"
                         Padding="12,10"
                         FontSize="14"
                         Background="Transparent"/>
            </Border>
        </StackPanel>

        <!-- Enable Loop Toggle -->
        <Grid Grid.Row="3" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Enable Loop"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#2D3436"/>
                <TextBlock Text="Auto run next row after completion"
                           FontSize="11"
                           Foreground="#636e72"
                           TextWrapping="Wrap"/>
            </StackPanel>
            <ToggleButton x:Name="EnableLoopToggle"
                          Grid.Column="1"
                          VerticalAlignment="Center"
                          IsChecked="False">
                <ToggleButton.Template>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid>
                            <Border x:Name="Background"
                                    Width="50" Height="26"
                                    CornerRadius="13"
                                    Background="#E0E0E0"
                                    HorizontalAlignment="Left"/>
                            <Ellipse x:Name="Thumb"
                                    Width="22" Height="22"
                                    Fill="White"
                                    HorizontalAlignment="Left"
                                    Margin="2,0,0,0">
                                <Ellipse.Effect>
                                    <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="4" ShadowDepth="1"/>
                                </Ellipse.Effect>
                            </Ellipse>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Background" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#667eea" Offset="0"/>
                                            <GradientStop Color="#764ba2" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,2,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </ToggleButton.Template>
            </ToggleButton>
        </Grid>

        <!-- Info -->
        <TextBlock Grid.Row="4"
                   Text="Excel file: Nick phu nth.xlsx (or single .xlsx in Templates/nth/signin)"
                   FontSize="11"
                   Foreground="#b2bec3"
                   TextWrapping="Wrap"
                   Margin="0,0,0,8"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="6"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button x:Name="CancelButton"
                    Content="Huy"
                    Width="100"
                    Height="44"
                    Margin="0,0,12,0"
                    Background="Transparent"
                    BorderBrush="#e1e8ed"
                    BorderThickness="2"
                    Foreground="#636e72"
                    FontWeight="SemiBold"
                    FontSize="14"
                    Cursor="Hand"
                    Click="CancelButton_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button x:Name="OkButton"
                    Content="Xac Nhan"
                    Width="140"
                    Height="44"
                    FontWeight="Bold"
                    FontSize="14"
                    Foreground="White"
                    Cursor="Hand"
                    Click="OkButton_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="8" Padding="16,0">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#56ab2f" Offset="0"/>
                                    <GradientStop Color="#a8e063" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect Color="#56ab2f" Opacity="0.4" BlurRadius="10" ShadowDepth="3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>
