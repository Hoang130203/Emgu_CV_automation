<Window x:Class="GameAutomation.UI.WPF.RegionSelectionOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Region"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#40000000"
        Topmost="True"
        WindowState="Maximized"
        Cursor="Cross"
        KeyDown="Window_KeyDown"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseMove="Window_MouseMove"
        MouseLeftButtonUp="Window_MouseLeftButtonUp">

    <Grid>
        <!-- Instructions at top -->
        <Border HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0"
                Background="#E0000000" CornerRadius="8" Padding="20,12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🎯 Drag to select search region" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                <TextBlock Text="  |  " Foreground="#808080" FontSize="14"/>
                <TextBlock Text="ESC to cancel" Foreground="#ff6b6b" FontSize="14"/>
            </StackPanel>
        </Border>

        <!-- Selection Rectangle -->
        <Canvas x:Name="SelectionCanvas">
            <Rectangle x:Name="SelectionRect"
                      Stroke="#00ff00"
                      StrokeThickness="2"
                      StrokeDashArray="5,3"
                      Fill="#3000ff00"
                      Visibility="Collapsed"/>
        </Canvas>

        <!-- Position info at bottom -->
        <Border x:Name="InfoPanel" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,20"
                Background="#E0000000" CornerRadius="8" Padding="16,10"
                Visibility="Collapsed">
            <StackPanel>
                <TextBlock x:Name="PixelInfo" Foreground="White" FontSize="12" FontFamily="Consolas"/>
                <TextBlock x:Name="RatioInfo" Foreground="#00ff00" FontSize="12" FontFamily="Consolas" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
